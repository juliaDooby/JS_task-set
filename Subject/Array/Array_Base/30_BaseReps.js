--------------------------------------------------------------------------------------------- 
⋙ ❍ Массивы:
---
30. Поиск максимального элемента в массиве
⌛ Задача: Найти максимальное число в массиве.
🎯 Решение:
0.1. Использование `Math.max()` и `spread`
```javascript
function findMax(arr) {
    return Math.max(...arr);
}

console.log(findMax([3, 5, 9, 1, 6])); // 9
```
Объяснение:
- Использует оператор `...` (spread), чтобы передать все элементы в `Math.max()`.
- Работает за O(N).
---------------------------------------------------------------------------------------------  
