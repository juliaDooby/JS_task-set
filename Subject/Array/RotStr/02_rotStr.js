---------------------------------------------------------------------------------------------  
⋙ ❍ Задача: Простая проверка поворота строки:
---
Условие:
2. ⌛ Задача:  С учётом пробелов.
Условие:  
Пробелы считаются значимыми. `"abc "` и `" cab"` — не ротация.
🎯 Решение:
```js
function isRotatedStrict(s1, s2) {
  if (s1.length !== s2.length) return false;
  return (s1 + s1).includes(s2);
}
```
Объяснение: Та же логика — важно, что пробелы остаются.
---------------------------------------------------------------------------------------------  
