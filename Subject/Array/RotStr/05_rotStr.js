---------------------------------------------------------------------------------------------  
⋙ ❍ Задача: Простая проверка поворота строки:
---
Условие:
5. ⌛ Задача: Учитывать Unicode символы.
Условие:  
Реализуй проверку без использования `includes()`.
🎯 Решение:
```js
function isRotatedUnicode(s1, s2) {
  if ([...s1].length !== [...s2].length) return false;
  return ([...s1, ...s1].join('')).includes([...s2].join(''));
}
```
Объяснение: Разбиваем строки на массивы символов с поддержкой Unicode (например, эмодзи).
--------------------------------------------------------------------------------------------- 
