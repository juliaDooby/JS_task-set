---------------------------------------------------------------------------------------------  
⋙ ❍ Задача:  Проверка с помощью регулярных выражений (чисто ради фана)
---
Условие:
10. ⌛ Задача: Проверка с ограниченным числом поворотов.
Условие:  
Проверь, может ли `s2` получиться из `s1` максимум за `k` циклических поворотов.
🎯 Решение:
```js
function isKRotated(s1, s2, k) {
  if (s1.length !== s2.length) return false;
  for (let i = 1; i <= k; i++) {
    const rotated = s1.slice(i) + s1.slice(0, i);
    if (rotated === s2) return true;
  }
  return false;
}
```
Пример:  
`isKRotated("abcdef", "defabc", 3)` → `false`,  
`isKRotated("abcdef", "bcdefa", 1)` → `true`
Объяснение: Используем регулярку — не самый практичный, но интересный способ.
--------------------------------------------------------------------------------------------- 
