---------------------------------------------------------------------------------------------  
⋙ ❍ Задача: Вернуть индекс поворота
---
Условие:
8. ⌛ Задача: Проверка всех возможных поворотов.
Условие:  
Если `s2` — поворот `s1`, верни индекс, с которого начинается поворот.
🎯 Решение:
```js
function getRotationIndex(s1, s2) {
  if (s1.length !== s2.length) return -1;
  const doubled = s1 + s1;
  return doubled.indexOf(s2);
}
```
Объяснение: Индекс появления `s2` в `s1 + s1` — и есть точка поворота. Если `-1`, значит не ротация.
---------------------------------------------------------------------------------------------  
