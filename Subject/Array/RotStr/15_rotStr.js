---------------------------------------------------------------------------------------------  
⋙ ❍ Задача:  Проверка с помощью регулярных выражений (чисто ради фана)
---
Условие:
15. ⌛ Задача: Является ли одна строка циклическим сдвигом другой (по символьным индексам).
Условие:
Проверь, можно ли получить `s2` из `s1`, сдвигая каждый символ вправо ровно на 1 (по модулю длины строки).
🎯 Решение:
```js
function isOneStepRotation(s1, s2) {
  if (s1.length !== s2.length) return false;
  const rotated = s1[s1.length - 1] + s1.slice(0, s1.length - 1);
  return rotated === s2;
}
```
Пример:  
`isOneStepRotation("abcde", "eabcd")` → `true`
Объяснение: Используем регулярку — не самый практичный, но интересный способ.
---------------------------------------------------------------------------------------------  
