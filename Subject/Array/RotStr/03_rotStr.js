---------------------------------------------------------------------------------------------  
⋙ ❍ Задача: Простая проверка поворота строки:
---
Условие:
3. ⌛ Задача: Удалить пробелы перед сравнением.
Условие:  
Сравниваем строки, но игнорируем пробелы.
🎯 Решение:
```js
function isRotatedNoSpaces(s1, s2) {
  s1 = s1.replace(/\s/g, '');
  s2 = s2.replace(/\s/g, '');
  if (s1.length !== s2.length) return false;
  return (s1 + s1).includes(s2);
}
```
Объяснение: `replace(/\s/g, '')` удаляет все пробелы перед сравнением.
---------------------------------------------------------------------------------------------  
