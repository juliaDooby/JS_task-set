---------------------------------------------------------------------------------------------  
⋙ ❍ Задача: Простая проверка поворота строки
---
Условие:
7. ⌛ Задача: Проверка всех возможных поворотов.
Условие:  
Верни `true`, если `s2` равна **любому** циклическому сдвигу `s1`.
🎯 Решение:
```js
function isAnyRotation(s1, s2) {
  if (s1.length !== s2.length) return false;
  for (let i = 0; i < s1.length; i++) {
    const rotated = s1.slice(i) + s1.slice(0, i);
    if (rotated === s2) return true;
  }
  return false;
}
```
Объяснение: Генерируем все сдвиги вручную.
---------------------------------------------------------------------------------------------  
