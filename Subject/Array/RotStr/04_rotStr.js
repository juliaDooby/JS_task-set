---------------------------------------------------------------------------------------------  
⋙ ❍ Задача: Простая проверка поворота строки:
---
Условие:
4. ⌛ Задача: Проверка без includes().
Условие:  
Реализуй проверку без использования `includes()`.
🎯 Решение:
```js
function isRotatedManual(s1, s2) {
  if (s1.length !== s2.length) return false;
  const doubled = s1 + s1;
  for (let i = 0; i <= doubled.length - s2.length; i++) {
    if (doubled.slice(i, i + s2.length) === s2) return true;
  }
  return false;
}
```
Объяснение: Проверка подстрок вручную через `slice()`.
---------------------------------------------------------------------------------------------  
