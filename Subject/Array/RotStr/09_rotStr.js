---------------------------------------------------------------------------------------------  
⋙ ❍ Задача:  Проверка с помощью регулярных выражений (чисто ради фана)
---
Условие:
9. ⌛ Задача: Проверка всех возможных поворотов.
Условие:  
Если `s2` — поворот `s1`, верни индекс, с которого начинается поворот.
🎯 Решение:
```js
function isRotatedRegex(s1, s2) {
  if (s1.length !== s2.length) return false;
  const regex = new RegExp(s2);
  return regex.test(s1 + s1);
}
```
Объяснение: Используем регулярку — не самый практичный, но интересный способ.
---------------------------------------------------------------------------------------------  
