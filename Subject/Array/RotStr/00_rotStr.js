---------------------------------------------------------------------------------------------  
⋙ ❍ Задача: Простая проверка поворота строки:
---
Условие:
0. ⌛ Задача: Проверь, является ли строка `s2` повёрнутой версией строки `s1`.
🎯 Решение:
```js
function isRotated(s1, s2) {
  if (s1.length !== s2.length) return false;
  return (s1 + s1).includes(s2);
}
```
Объяснение: Если строка `s2` — повёрнутая версия `s1`, то она будет подстрокой `s1 + s1`.  
Например: `"abcde"` + `"abcde"` → `"abcdeabcde"` содержит `"deabc"`.
---------------------------------------------------------------------------------------------  
