---------------------------------------------------------------------------------------------  
⋙ ❍ Задача:  Проверка с помощью регулярных выражений (чисто ради фана)
---
Условие:
11. ⌛ Задача: Найти минимальное количество поворотов для совпадения.
Условие:  
Верни минимальное число поворотов, чтобы `s1` стал равен `s2`, или `-1`, если это невозможно.
🎯 Решение:
```js
function minRotations(s1, s2) {
  if (s1.length !== s2.length) return -1;
  for (let i = 0; i < s1.length; i++) {
    const rotated = s1.slice(i) + s1.slice(0, i);
    if (rotated === s2) return i;
  }
  return -1;
}
```
Пример:  
`minRotations("abcdef", "defabc")` → `3`
Объяснение: Используем регулярку — не самый практичный, но интересный способ.
---------------------------------------------------------------------------------------------  
