---------------------------------------------------------------------------------------------  
⋙ ❍ Задача:  Проверка с помощью регулярных выражений (чисто ради фана)
---
Условие:
19. ⌛ Задача: Найти "наилучшую" ротацию по алфавиту.
Условие:
Верни ротацию строки, которая будет лексикографически наименьшей.
🎯 Решение:
```js
function bestRotationLex(s) {
  let best = s;
  for (let i = 1; i < s.length; i++) {
    const rotated = s.slice(i) + s.slice(0, i);
    if (rotated < best) best = rotated;
  }
  return best;
}
```
Пример:  
`bestRotationLex("bca")` → `"abc"`
---------------------------------------------------------------------------------------------
