---------------------------------------------------------------------------------------------  
⋙ ❍ Задача:  Проверка с помощью регулярных выражений (чисто ради фана)
---
Условие:
16. ⌛ Задача: Я Найти количество различных ротаций строки.
Условие:
Посчитай, сколько уникальных ротаций строки существует.
🎯 Решение:
```js
function countUniqueRotations(s) {
  const set = new Set();
  for (let i = 0; i < s.length; i++) {
    const rotated = s.slice(i) + s.slice(0, i);
    set.add(rotated);
  }
  return set.size;
}
```
Пример:  
`countUniqueRotations("aaaa")` → `1`  
`countUniqueRotations("abcd")` → `4`
Объяснение: Используем регулярку — не самый практичный, но интересный способ.
---------------------------------------------------------------------------------------------  
