---------------------------------------------------------------------------------------------  
⋙ ❍ Vue 2 и/или Vue 3 (реактивность, Composition API):
---
14. Использование `v-for` для отображения данных из массива
⌛ Задача: Отобразите список пользователей из массива с помощью `v-for`.
🎯 Решение:
```vue
<template>
  <div>
    <ul>
      <li v-for="user in users" :key="user.id">
        {{ user.name }}
      </li>
    </ul>
  </div>
</template>

<script>
export default {
  data() {
    return {
      users: [
        { id: 1, name: 'Alex' },
        { id: 2, name: 'Ivan' },
        { id: 3, name: 'Joe' },
      ],
    };
  },
};
</script>
```
Объяснение:
- `v-for` используется для итерации по массиву и вывода данных в шаблоне.
---------------------------------------------------------------------------------------------  
