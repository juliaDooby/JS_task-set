--------------------------------------------------------------------------------------------
⋙ ❍ Список примитивных типов ?
---
В JavaScript есть 7 примитивных типов:  
1. `string` – строка  
   ```javascript
   let name = "JavaScript";
   ```  
2. `number` – число (целые и с плавающей точкой)  
   ```javascript
   let age = 25;
   let pi = 3.14;
   ```  
3. `bigint` – большие числа (для работы с числами больше `2^53-1`)  
   ```javascript
   let bigNumber = 9007199254740991n;
   ```  
4. `boolean` – логический тип (истина/ложь)  
   ```javascript
   let isActive = true;
   ```  
5. `undefined` – значение переменной, которая была объявлена, но не инициализирована  
   ```javascript
   let x;
   console.log(x); // undefined
   ```  
6. `null` – отсутствие значения  
   ```javascript
   let emptyValue = null;
   ```  
7. `symbol` – уникальный идентификатор  
   ```javascript
   let uniqueId = Symbol("id");
   ```  
Примитивные типы не являются объектами и хранятся по значению (а не по ссылке, как объекты). 
--------------------------------------------------------------------------------------------
