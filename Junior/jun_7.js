--------------------------------------------------------------------------------------------
⋙ ❍ Библиотеки следует добавлять раньше, чем скрипты, которые их используют ?
---  
Например, следующий фрагмент приведет к ошибке «Uncaught ReferenceError: jQuery is not defined»
<script src="js/jquery.easing.js"></script>
<script src="js/jquery.js"></script>  

Да, библиотеки следует подключать раньше, чем скрипты, которые их используют.  

Почему?  
1. Зависимости должны быть загружены до их использования  
   - Если ваш код (`script.js`) использует jQuery, но jQuery ещё не загружен, это вызовет ошибку (`Uncaught ReferenceError`).  

2. Порядок загрузки важен  
   - Браузер выполняет скрипты в порядке их подключения.  
   - Если библиотека загружается после скрипта, который её использует, этот скрипт не сможет найти нужные функции.  

Правильный порядок подключения:
```html
<!-- Подключаем библиотеку сначала -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>

<!-- Затем используем её в скрипте -->
<script src="script.js"></script>
```

Альтернативный вариант: `defer`
Если подключаете скрипты через `<script defer>`, браузер загружает их асинхронно, но выполняет в порядке их расположения:

```html
<script src="library.js" defer></script>
<script src="app.js" defer></script>
```
Это тоже гарантирует, что библиотека загрузится до её использования.
--------------------------------------------------------------------------------------------  
